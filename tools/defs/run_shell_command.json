{
  "name": "run_shell_command",
  "description": "Execute a shell command in the working directory and return stdout, stderr, and exit code. Use for running tests, build commands, installing packages, linters, and other CLI operations. Commands time out after 60 seconds by default.",
  "parameters": {
    "type": "object",
    "properties": {
      "command": {
        "type": "string",
        "description": "The shell command to execute. Can include pipes, redirects, and chained commands with && or ||."
      },
      "working_dir": {
        "type": "string",
        "description": "Working directory in which to execute the command. Defaults to the project root.",
        "default": "."
      },
      "timeout_seconds": {
        "type": "integer",
        "description": "Maximum time to wait for the command to complete, in seconds. Defaults to 60.",
        "default": 60
      },
      "env": {
        "type": "object",
        "description": "Optional dictionary of additional environment variables to set for this command.",
        "additionalProperties": {
          "type": "string"
        }
      },
      "dir_path": {
        "type": "string",
        "description": "Alias for working_dir. If both are provided, dir_path takes precedence."
      },
      "shell_preference": {
        "type": "string",
        "description": "Preferred shell to execute in. Use 'auto' (default), 'powershell', 'pwsh', 'cmd', 'bash', 'sh', or 'zsh'.",
        "default": "auto"
      },
      "reject_on_shell_mismatch": {
        "type": "boolean",
        "description": "If true, do not run when command style appears incompatible with selected shell.",
        "default": true
      },
      "auto_switch_shell": {
        "type": "boolean",
        "description": "If true (default), automatically switch to a compatible available shell when style mismatch is detected.",
        "default": true
      },
      "enforce_workspace_write_policy": {
        "type": "boolean",
        "description": "When true, mutating shell commands must use verifiable write targets inside OVERLORD11_TASK_DIR. If omitted, defaults to true when task context is present.",
        "default": true
      },
      "describe_environment": {
        "type": "boolean",
        "description": "If true, prefix output with a summary of the current shell environment (OS, shell, env vars).",
        "default": false
      }
    },
    "required": ["command"]
  }
}
