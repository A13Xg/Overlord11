{
  "name": "file_management",
  "description": "Allows the agent to read from or write to the local file system within the project directory for saving drafts and outputs.",
  "parameters": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": ["read", "write", "append"],
        "description": "The file operation to perform."
      },
      "file_path": {
        "type": "string",
        "description": "The relative path to the file (e.g., 'output/draft.md', 'input/source.txt')."
      },
      "content": {
        "type": "string",
        "description": "The text content to write or append. Leave empty if action is 'read'."
      }
    },
    "required": ["action", "file_path"]
  }
}
