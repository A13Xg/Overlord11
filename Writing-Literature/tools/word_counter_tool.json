{
  "name": "word_counter",
  "description": "Analyzes text to provide word count, character count, and reading time estimates.",
  "parameters": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string",
        "description": "The text content to analyze."
      },
      "include_stats": {
        "type": "boolean",
        "description": "If true, returns additional statistics (sentences, paragraphs, avg sentence length)."
      }
    },
    "required": ["text"]
  }
}
