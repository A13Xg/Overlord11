{
  "name": "file_management",
  "description": "Manage files in the workspace. Supports reading, writing, listing, and deleting files. All paths are relative to the session workspace directory.",
  "parameters": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": ["read", "write", "list", "delete", "exists", "mkdir"],
        "description": "The file operation to perform"
      },
      "path": {
        "type": "string",
        "description": "Relative path within the workspace (e.g., 'src/main.py' or 'tests/')"
      },
      "content": {
        "type": "string",
        "description": "Content to write (required for 'write' action)"
      },
      "recursive": {
        "type": "boolean",
        "description": "For 'list' action: include subdirectories. For 'mkdir': create parent directories",
        "default": false
      }
    },
    "required": ["action", "path"]
  }
}
