{
  "name": "link_validator",
  "description": "Physically verifies if a URL is active and checks it against the blacklist defined in config.json.",
  "parameters": {
    "type": "object",
    "properties": {
      "url": {
        "type": "string",
        "description": "The full URL to verify (e.g., https://arxiv.org/abs/1234.5678)."
      },
      "check_relevance": {
        "type": "boolean",
        "description": "If true, the tool will attempt to scrape the page title to ensure it matches the research topic."
      }
    },
    "required": ["url"]
  }
}