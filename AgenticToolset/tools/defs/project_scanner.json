{
  "name": "project_scanner",
  "description": "Deep project analysis tool. Scans a directory to detect programming languages, frameworks, entry points, config files, dependency files, and directory structure. Returns a comprehensive JSON profile of the project. Run this first when dropped into a new codebase.",
  "parameters": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "Root directory path to scan (default: current directory)",
        "default": "."
      },
      "depth": {
        "type": "integer",
        "description": "Maximum directory traversal depth (default: 5)",
        "default": 5
      },
      "output": {
        "type": "string",
        "description": "Optional file path to write the scan result JSON"
      },
      "compact": {
        "type": "boolean",
        "description": "Output compact JSON without indentation",
        "default": false
      }
    },
    "required": ["path"]
  }
}
