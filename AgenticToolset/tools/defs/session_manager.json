{
  "name": "session_manager",
  "description": "Manages work sessions with unique IDs. Creates session workspaces, tracks file changes, records agent and tool usage, and generates session summaries. Use this to maintain continuity across a work session.",
  "parameters": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "description": "Session action to perform",
        "enum": ["create", "status", "log_change", "log_agent", "log_tool", "add_note", "close", "list", "active"]
      },
      "session_id": {
        "type": "string",
        "description": "Session identifier (format: YYYYMMDD_HHMMSS)"
      },
      "description": {
        "type": "string",
        "description": "Description for create/close actions"
      },
      "data": {
        "type": "object",
        "description": "Additional data payload. For log_change: {file, action, summary}. For add_note: {note}."
      },
      "status_filter": {
        "type": "string",
        "description": "Filter sessions by status (active, closed)"
      }
    },
    "required": ["action"]
  }
}
