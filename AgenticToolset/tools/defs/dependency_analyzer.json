{
  "name": "dependency_analyzer",
  "description": "Analyzes project dependencies from package manifests (requirements.txt, package.json, Cargo.toml, go.mod, etc). Detects security concerns, unpinned versions, missing lockfiles, duplicate dependencies, and cross-manifest conflicts.",
  "parameters": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "Project root path to analyze",
        "default": "."
      },
      "check": {
        "type": "string",
        "description": "Type of analysis to perform",
        "enum": ["all", "security", "warnings", "conflicts"],
        "default": "all"
      },
      "output": {
        "type": "string",
        "description": "Optional file path to write analysis result"
      }
    },
    "required": ["path"]
  }
}
