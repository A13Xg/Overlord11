# Overlord11 Backend Requirements
# Install with: pip install -r requirements-backend.txt

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
python-multipart>=0.0.9
websockets>=12.0

# Provider SDKs (install as needed based on your active provider)
anthropic>=0.40.0
openai>=1.50.0
google-generativeai>=0.8.0

# Existing project dependencies (already present in base requirements)
# requests>=2.31.0
